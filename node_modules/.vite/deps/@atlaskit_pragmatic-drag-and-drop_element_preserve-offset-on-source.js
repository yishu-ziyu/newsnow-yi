import "./chunk-4B2QHNJT.js";

// node_modules/.pnpm/@atlaskit+pragmatic-drag-and-drop@1.6.1/node_modules/@atlaskit/pragmatic-drag-and-drop/dist/esm/public-utils/element/custom-native-drag-preview/preserve-offset-on-source.js
function preserveOffsetOnSource(_ref) {
  var element = _ref.element, input = _ref.input;
  return function(_ref2) {
    var container = _ref2.container;
    var sourceRect = element.getBoundingClientRect();
    var containerRect = container.getBoundingClientRect();
    var offsetX = Math.min(
      // difference
      input.clientX - sourceRect.x,
      // don't let the difference be more than the width of the container,
      // otherwise the pointer will be off the preview
      containerRect.width
    );
    var offsetY = Math.min(
      // difference
      input.clientY - sourceRect.y,
      // don't let the difference be more than the height of the container,
      // otherwise the pointer will be off the preview
      containerRect.height
    );
    return {
      x: offsetX,
      y: offsetY
    };
  };
}
export {
  preserveOffsetOnSource
};
//# sourceMappingURL=@atlaskit_pragmatic-drag-and-drop_element_preserve-offset-on-source.js.map
