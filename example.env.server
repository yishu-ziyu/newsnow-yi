G_CLIENT_ID=
G_CLIENT_SECRET=
JWT_SECRET=
INIT_TABLE=true
ENABLE_CACHE=true
PRODUCTHUNT_API_TOKEN=

# Agent trackers（Layer 3a）
# 外部调度器打 POST /api/agent/trackers/run 时要带的密钥
CRON_SECRET=
# 自托管时可让进程自己跑到期追踪（默认关）
ENABLE_TRACKER_SCHEDULER=false
TRACKER_TICK_MS=60000
